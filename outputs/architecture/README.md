# NURA ERP Architecture Outputs

Place the current architecture Parts / canonical deliverables here.

Prefer the current working/canonical version of each Part.

Use Git history for ordinary version history instead of keeping many duplicate `FINAL_v2_revised_last` files. Preserve older standalone versions only when they carry material lineage not captured by Git.
